"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        const name = "MUSTHOFA SIFA'UL Q";
        const balance = 140306;
        const isVip = true;
        // GUNAKAN COMMAND : npc tsc --watch
        // name = 100;
        // name = "budi";
        // name = 100;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
